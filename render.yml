services:
  - type: web
    name: bundles
    env: ruby
    buildCommand: "bundle install && bundle exec rake assets:precompile && bundle exec rake db:migrate && bundle exec rails assets:precompile"
    startCommand: "bundle exec puma -C config/puma.rb"
    envVars:
      - key: RAILS_MASTER_KEY
        sync: false
      - key: RAILS_SERVE_STATIC_FILES
        value: "true"
      - key: RAILS_ENV
        value: production
      - key: RAILS_LOG_TO_STDOUT
        value: "true"
      - key: DATABASE_URL
        fromDatabase:
          name: bundles-db
          property: connectionString
    plan: free

databases:
  - name: bundles-db
    databaseName: bundles
    user: bundles
    plan: free
